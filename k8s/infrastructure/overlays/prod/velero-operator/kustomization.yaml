apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

helmCharts:
  - name: velero
    repo: https://vmware-tanzu.github.io/helm-charts
    version: "8.0.0"
    releaseName: velero
    namespace: velero
    includeCRDs: true
    valuesFile: ../../../base/backup/velero-operator/values.yaml
    additionalValuesFiles:
      - values.yaml
