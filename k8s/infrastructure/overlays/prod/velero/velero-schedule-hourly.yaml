# Database backup - hourly (prod-only)
# Staging uses daily-full-backup only
apiVersion: velero.io/v1
kind: Schedule
metadata:
  name: hourly-database-backup
  namespace: velero
spec:
  schedule: "0 * * * *"
  template:
    includedNamespaces:
      - openpanel
    labelSelector:
      matchLabels:
        backup: database
    ttl: 24h0m0s
    storageLocation: default
