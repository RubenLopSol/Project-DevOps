# =============================================================================
# Alertmanager — STAGING overrides (Minikube-sized).
# =============================================================================

resources:
  requests:
    cpu: 20m
    memory: 32Mi
  limits:
    cpu: 50m
    memory: 64Mi

persistence:
  storageClass: local-path
  size: 1Gi
