
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# Prod uses AWS S3 (bucket + IAM Role provisioned by
# terraform/environments/prod, accessed via IRSA) as Velero's backup target,
resources:
  - velero
